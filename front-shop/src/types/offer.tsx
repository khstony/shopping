export interface Offer {
  offerId: number;
  uploader: number;
  productName: string;
  productImage: string;
  productDesc: string;
  productPrice: number;
  discountRate: number;
  stock: number;
}